<div class="page-header">
  <h1></h1>
</div>




<div class="table-responsive">
  <% @grouped.each do |k, v| %>
    Тикет
  <%= k.first.auditable_id %>
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Тип</th>      
        <th>Пользователь</th>
        <th>Изменения</th>
        <th>Версия</th>
        
      </tr>
    </thead>
    
    <% k.each do |i, j| %>
        <tbody>      
            
            <td><%= i.auditable_type %></td>            
            <td><%= i.user.name %></td>
            <td><%= i.audited_changes%></td>
            <td><%= i.version%></td>      
        </tbody>
    <% end %>
  </table>
  <% end %>
  
</div>
